---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description?: string;
  backHref?: string;
  backLabel?: string;
  active?: string;
}

const {
  title,
  description,
  backHref = '/blog',
  backLabel = 'Back to writing',
  active,
} = Astro.props;
---
<BaseLayout title={`${title} — Amey Bhavsar`} description={description} active={active}>
  <div class="prose-wrapper">
    <a href={backHref} class="prose-back">
      <svg width="14" height="14" viewBox="0 0 14 14" fill="none">
        <path d="M11 7H3M3 7l4-4M3 7l4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      {backLabel}
    </a>
    <slot />
  </div>
</BaseLayout>
